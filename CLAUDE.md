# 길이 척척 (WTPP SNAP)

조각을 이어 붙여 다리를 놓는 6~9세용 길이 개념 게임.
상위 규칙은 `Wetipapa/CLAUDE.md`를 따르고, 여기에는 이 게임에만 해당하는 것만 적는다.

- 저장소: `wetipapa/theo-length`
- 스택: Vite + React 19 + Tailwind v4 (캔버스 없이 DOM/CSS로만 그린다)
- 배포 예정 주소: `length.wetipapa.com`

## 이 게임에서 무엇을 배우는가

**길이는 정해진 단위가 반복된 양이라는 감각.**

아이가 흔히 하는 실수는 자의 숫자를 읽는 것으로 끝내는 것이다.
그래서 자의 눈금을 셀 것이 아니라 **눈금 사이의 칸**을 세도록 만들었다 —
자 안쪽에 한 칸씩 옅은 줄무늬를 넣고, 조각에도 칸 경계를 그린다.

목표 길이를 만드는 방법이 여러 가지라는 것도 중요하다.
8칸은 4+4로도, 2+2+2+2로도, 5+3으로도 만들어진다. 트레이에는 늘 답이 여럿 있다.

## 단위는 "칸"이지 cm가 아니다

화면의 한 칸은 기기마다 실제 크기가 다르다. cm라고 부르면 아이가
"화면에서 이만큼이 1cm"라고 잘못 배운다. 그래서 게임 안에서만 통하는 **칸**을 쓴다.

`unitPx`는 문제마다 화면 폭에 맞춰 다시 계산한다. 어느 기기에서든 다리 하나가
화면에 꽉 차게 들어오고, 칸 수는 그대로다.

## 문제는 열두 가지, 조작은 둘

| 종류 | 무엇을 배우나 | 나오는 단계 |
|---|---|---|
| `count` | 숫자로 알려준 길이만큼 놓기 | 쉬움 |
| `measure` | 자 위 물체를 재서 스스로 알아내기 | 쉬움 |
| `blank` | 눈금 없는 조각을 숫자만 보고 다루기 | 보통 |
| `offset` | 0이 아닌 눈금에서 시작하는 물체 재기 | 어려움 |
| `repeat` | 한 조각 × 개수 → **전체** | 쉬움 |
| `countUnit` | 전체 ÷ 한 조각 → **개수** | 쉬움 |
| `sameParts` | 전체 ÷ 개수 → **한 조각** | 보통 |
| `unitOnly` | 같은 길이를 다른 단위로 재면 개수가 달라진다 | 보통 |
| `segments` | A-B-C-D 선분에서 모르는 구간 구하기 | 어려움 |
| `cancelPair` | 양쪽에 똑같이 있는 것을 지우고 남은 것 견주기 | 어려움 |
| `bentPath` | 꺾여 있어도 길이는 그대로 | 어려움 |
| `midpoint` `tickGap` | 수직선도 자와 같은 구조다 | 어려움 |

**조작은 둘뿐이다.** 열 가지가 "틈을 정확히 채운다"로 끝나고, 수직선 둘만 눈금을 짚는다.
문제 종류가 늘어도 아이가 조작을 다시 배우지 않게 하려고 이렇게 묶었다.
`cancelPair`도 지우기가 끝나면 결국 틈을 채운다.

새 종류를 넣을 때 손댈 곳은 `lib/problems.ts`의 `makeProblem` 하나다.

### 단위 반복은 세 방향을 다 낸다

`repeat` 하나만 연습하면 아이는 이걸 곱셈 문제로 외우고, 방향이 바뀌면 손을 못 댄다.
그래서 셋 중 무엇을 모르는지가 매번 달라지게 한다.

`sameParts`는 조각 하나를 누르면 **정해진 개수만큼 한꺼번에** 놓인다.
한 개씩 놓게 하면 그냥 채우다 맞추게 되고, 그 조각들이 다 같은 길이라는 게 안 보인다.
여기서는 여러 크기를 대 보는 것이 곧 푸는 방법이라, **빼도 보너스를 잃지 않는다.**

### 눈금을 언제 지우나

셀 수 있으면 아이는 관계를 따지지 않고 칸부터 센다. 그래서 종류마다 다르게 준다.

| 종류 | 눈금 | 숫자 | 왜 |
|---|---|---|---|
| `blank` | 없음 | 있음 | 주어진 길이를 그대로 받아들이는 연습 |
| `unitOnly` | 없음 | **없음** | 숫자가 보이면 "6칸이니까 2개"로 계산해 버린다 |
| `segments` 선분 | 없음 | 위쪽 호에만 | 칸을 세면 호를 볼 이유가 없다 |
| `cancelPair` 두 줄 | 없음 | 아는 것만 | 셀 수 있으면 지울 이유가 없다 |

### 난이도는 크기가 아니라 개념이다

일곱 살에게 어려운 것은 14칸이 아니라 "전체와 개수를 알 때 한 조각을 되짚는" 방향이다.
그래서 단계마다 새 개념을 한둘씩만 얹고, 앞 단계 개념은 계속 섞어 낸다.
`도전`에는 **새 조작이 하나도 없다** — 처음 보는 조작까지 나오면 개념이 아니라 조작에서 막힌다.

`kinds`는 순서대로 돌려 낸다. 문제 수가 목록보다 많아 한 바퀴 돌면 끝과 처음이 이어 붙으므로,
**마지막 종류는 첫 종류와 달라야 한다.** 같으면 같은 문제를 두 판 연속 만난다.

## 문제 밑에 안내 줄을 상시로 띄우지 않는다

`8칸짜리 다리를 놓아요` 바로 밑에 `8칸 더 필요해요`를 띄우고 있었다.
문제를 두 번 읽는 꼴이고, 조각을 눌러 보면 알 것을 굳이 적은 것이다.

- **평소에는 문제 하나만 크게 띄운다.** 그 아래 상시 안내 줄은 두지 않는다
- **조작이 평소와 다른 판에서만** 한 줄 붙인다 (두 줄 견주기의 지우기, 수직선의 화살표)
- **맞히면 문제 카드가 그대로 칭찬 자리가 된다.** 따로 띄우면 눈이 두 군데를 봐야 한다

## 강과 언덕은 화면 끝까지 그린다

상판 폭만큼만 땅을 그렸더니 화면 가운데에 직사각형 세 개가 떠 있는 그림이 됐다.
욕조처럼 보이고, 일곱 살이 보면 여기가 어디인지 알 수 없다.

- **언덕은 좌우 화면 끝까지, 바닥까지 내린다.** 땅은 잘려 있지 않다. 위에 풀을 얹는다
- **강은 상판 아래에서 시작한다.** 채워야 할 틈까지 물빛으로 칠하면
  "비어 있다"가 아니라 "물이 차 있다"로 읽힌다
- **수레는 크게, 상판 위에 올라서게.** 위쪽에 여백을 두지 않으면 문제 카드를 파고든다

## 답을 화면에 적지 않는다

`tellsTarget`이 거짓인 문제 — 재고, 세고, 따져서 알아내야 하는 것들 —
에서는 **남은 칸 수를 화면 어디에도 적지 않는다.**

한동안 모든 문제에 `5칸 더 필요해요`를 띄우고 있었다.
`겹치는 구간을 보고 B~C를 채워요`라고 물어 놓고 바로 아래에 답을 적어 둔 셈이라,
아이는 호를 볼 이유가 없었다.

- 알려주는 문제(`count`·`blank`·`sameParts`·`countUnit`)는 남은 칸을 적어도 된다.
  문구에 이미 있는 수라 뺄셈을 거들 뿐이다
- 알려주지 않는 문제는 **지금까지 놓은 만큼**을 보여 준다.
  아이가 센 것을 확인해 주되 답은 말하지 않는다
- **조각을 흐리게 하는 것도 답을 흘린다.** 5는 흐린데 4는 아니면 남은 칸이 4라는 뜻이다.
  그래서 알려주는 문제에서만 흐리게 한다. 안 들어가는 조각은 대 봐도 벌점이 없다

## 답이 반드시 존재한다

트레이를 아무 조각으로 채우면 목표를 못 만드는 판이 생기고, 아이는 자기가 못 푸는 줄 안다.
그래서 `solutionFor()`로 **답이 되는 조합을 먼저 만들고** 그 조각들을 트레이에 반드시 넣는다.
나머지는 고민할 거리로 섞는 여분이다.

## 틀려서 끝나지 않는다

하트가 없다. 한 판은 문제 수로 끝난다.

- 남은 틈보다 긴 조각은 **아예 들어가지 않는다.** 다리가 흔들릴 뿐 벌점이 없다.
  넘치게 놓은 뒤 틀렸다고 하는 것보다, 애초에 안 들어가는 편이 배우기 쉽다
- 놓은 조각은 눌러서 언제든 뺀다
- 되돌리기를 벌하지 않는다. 빼고 다시 놓는 것도 길이를 재는 과정이다.
  다만 한 번에 맞히면 보너스를 줘서 다시 해볼 이유를 만든다

## 판정은 자동이다

합이 목표와 같아지는 순간 바로 다리가 완성된다.
"확인" 버튼을 두면 맞혀 놓고도 한 단계를 더 거쳐야 해서 손맛이 죽는다.

## 웨티는 어디에 나오나

제목에는 붙이지 않는다 — `길이 척척`이지 `웨티 길이 척척`이 아니다.
형제 서비스와 같은 규칙이다.

그림으로는 상위 표준을 그대로 따른다 — **첫 화면 컷과 끝 화면 얼굴 배지, 기록 사진.**
게임 화면에는 넣지 않았다. 여기서 성공을 알리는 것은 수레가 다리를 건너는 장면이고,
그 옆에 반응 얼굴을 띄우면 서로 시선을 뺏는다.

## 하지 말 것

- 칸을 cm라고 부르지 않는다
- 조각 색과 길이를 묶지 않는다. 같은 색이 늘 3칸이면 아이는 세지 않고 색만 본다
  (`PIECE_COLORS`는 매 문제 다시 섞는다)
- 조각 숫자를 가운데 두지 않는다. 칸 경계선을 덮어서 셀 수 없게 된다
- 수 뒤의 조사를 `과`로 고정하지 않는다. 읽는 소리로 정해진다 (`12와`, `13과` — `withParticle`)
- 겹치는 구간의 답 자리를 고정하지 않는다. 고정이면 아이가 따지지 않고 "가운데 칸"을 외운다
- 틀렸다고 게임을 끊지 않는다
